# dd-danmaku 全仓优化审查

> 审查时间：2026-08-04。原则：KISS / YAGNI / SOLID；先修正确性和容量风险，再拆热点，不做大重写。

## 1. 审查范围与现状

- 范围：`data-center/src`、`data-center/web/src`、`worker`、根目录 `ede.js`、CI 与数据库运行指标。
- 已验证：`python -m compileall -q data-center/src`、`node --check worker/cf_worker.js` 均通过；IDE 无当前诊断。
- 自动化测试现状：仓库未发现有效 `tests/`、pytest 配置或 JS 单测；CI 仅构建 Docker，没有 lint/test 门禁（`.github/workflows/docker-build.yml`）。
- 运行态健康：事件循环延迟约 4.53ms、队列无积压、DB 连接池无占用，暂不需要盲目提高并发。
- 体积热点：`ede.js` 约 8509 行、`worker/cf_worker.js` 约 3447 行、`media_meta_service.py` 约 1088 行。

## 2. P0：先修正确性与契约问题

### 2.1 本地别名兜底运行错误（已修复，待结构收口）

发现时的证据：模型类叫 `EpisodeLink`，表名才是 `episode_links`（`data-center/src/models_v2/cache.py:142-152`、`data-center/src/models_v2/__init__.py:17,46`）；兜底代码却导入不存在的 `EpisodeLinks`，线上日志已出现 `cannot import name 'EpisodeLinks'`。

同一段还曾读取错误字段：真实字段为 `episode_number`、`episode_title`、`dandan_anime_id`、`dandan_episode_id`（`cache.py:150-157`），`MediaLibrary` 使用 `type_code/type_desc`（`cache.py:184-185`）。

本次已修：改用 `EpisodeLink`，按 `dandan_anime_id` 批量查询，返回真实 `dandan_episode_id/episode_title`，只允许 `status=approved` 别名，并统一两条 Worker 429 路径的 dandanplay 响应外壳。

后续收口：不要长期在 `alias_external_service` 手工拼响应。改为“别名解析出 canonical/anime_id → 复用 `EntityAssembleService` 组装真实同构响应”。现有组装器已保留原始 `raw_json`、完整性校验和标准响应外壳（`data-center/src/services_v2/entity_assemble.py:101-121,152-170`）。

### 2.2 Worker 两个 429 兜底分支返回结构不一致

- 密钥池耗尽分支包装为 `{animes: fallbackResp.data}`（`worker/cf_worker.js:2968-3000`）。
- 上游返回 429 分支直接 `JSON.stringify(fallbackResp.data)`（`worker/cf_worker.js:3118-3144`）。

处理：提取唯一函数 `tryLocalSearchFallback(ctx)`，统一返回完整 dandanplay 结构：`{hasMore:false, animes:[], errorCode:0, success:true}`；两个分支只负责调用与返回，禁止复制 RPC、日志、响应头代码。

### 2.3 别名生效状态口径不一致

线上标准解析只允许 `status=approved`（`media_meta_service.py:606-668`），临时兜底查询未过滤状态（`alias_external_service.py:280-284`），可能把 pending/rejected 别名直接用于线上返回。

处理：所有线上别名解析只能调用 `media_meta_service.resolve_search_term` 或新的同层方法，禁止其他服务自行查询 `MediaAlias`。

### 2.4 日志迁移后的旧文案与接口契约残留

`control_client._handle_log_report` 注释仍写“落库 worker_request_logs”（`control_client.py:414-417`）；Dashboard 仍请求 `/dashboard/insights?hours=24`（`data-center/web/src/views/Dashboard.vue:419`），后端已改为当日/date 口径。

处理：更新注释和前端请求；API 响应增加 `stat_date` 展示，避免使用者误以为仍是最近 24 小时。

## 3. P1：容量与数据库优化

### 3.1 `control_messages` 已成为最大容量风险

运行数据（2026-08-05 MCP 复核）：约 153.2 万行 / 746.8MB，占数据库 49%；`api_cache_access_logs` 约 101.8 万行 / 260.6MB，占 17.1%。`control_messages` 现网已人工调整为保留 2 天，`api_cache_access_logs` 仍保留 30 天。

处理进度：
1. `control_messages` 保留期已由用户配置降至 2 天；`api_cache_access_logs` 调整为 14 天，并按最终决策继续全量保存所有访问类型。
2. 已完成 `control_messages` 审计分级：失败/超时和业务消息全量保存；已有专门数据源的高频成功 `cache.get/cache.upsert/log.report/metrics.report/stats.report/keypool.report` 按每类 1% 确定性采样。
3. 给运维页展示“估算每日增长量”和“预计占用”仍待处理，避免再次无感膨胀。

### 3.2 清理 SQL 需要稳定索引顺序

MCP 实测清理 SQL 原为按 `created_at < cutoff LIMIT 5000` 取 `control_messages.id`，单次约 0.7–1 秒；生产表缺少时间索引，`EXPLAIN` 显示全扫约 152 万行并 `Using filesort`。

已处理：模型和幂等补丁增加 `(created_at, id)` 复合索引；取 ID 改为 `ORDER BY created_at, id LIMIT batch`，继续使用跨 MySQL/SQLite/PG 的 SELECT+DELETE 分批方案。暂不切 MySQL 专属 DELETE 语法，保持实现简单和跨方言一致。

### 3.3 启动补丁文件持续膨胀

原 `database_patches.py` 已膨胀到 700 余行，所有历史补丁每次启动遍历，还包含不可逆删表补丁；生产日志进一步证实旧 `worker_request_logs` 被 `create_all()` 重建后又在每次启动删除。

已处理：补丁按索引、结构、回填、策略、不可逆领域拆到 `db_patches/`；新增 `schema_patch_history`，普通补丁首次成功检查后记录并在后续启动直接跳过。不可逆补丁默认关闭，只有 `ALLOW_DESTRUCTIVE_DB_PATCHES=true` 且确认备份后才进入执行列表。已移除 `WorkerRequestLog` 僵尸 ORM 模型，停止每次启动 `create_all` 重建旧表后再删除的重复 DDL。

### 3.4 Worker 文件日志搜索的边界需明确

单文件 20MB、最多扫描 20 万行（`worker_log_file_service.py:23-26,185-215`），有筛选时每次翻页都会从文件尾重新扫描。

已处理：维持轮转文件与单文件检索方案，不引入 Elasticsearch/FTS。服务端增加按文件身份缓存的元信息（体积、行数、首末时间），当前文件追加时只扫描新增字节，并处理半行、截断、同尺寸重写和轮转改名边界；前端文件下拉默认限定当前单文件，并展示体积、行数和时间范围。高频统计继续依赖每日聚合表。

## 4. P1：结构瘦身

### 4.1 拆 Worker 的超长 `handleRequest`

`handleRequest` 从 `worker/cf_worker.js:2156` 延伸到约 3388，混合路由、鉴权、签名、限流、缓存、R2、密钥池、回源与日志，是首要维护热点。

最小拆分（不改行为）：
- `parseRequestContext`：URL、body、客户端身份。
- `authorizeRequest`：IP/UA/用户/签名。
- `tryEdgeCaches`：内存、本地端、R2、负缓存。
- `forwardUpstream`：密钥选择、签名、重试。
- `tryLocalFallback`：429/配额耗尽统一兜底。
- `finalizeResponse`：裁集、响应头、日志和指标。

先提纯函数并写契约测试，不急着拆多个文件；函数稳定后再按 `worker/modules/` 分文件。

第一批已处理：仍保留单文件结构，新增 `createRequestContext`、`readRequestBodyText`、`parseProxyTarget`、`prepareEpisodeRequest` 四个低耦合函数；入口共享字段和请求体读取已收口，代理 URL 错误分类、非法路由计数/封禁、主机白名单、数字 episode 剥离的顺序与响应保持不变。临时 Node 契约测试覆盖 IP 头优先级、请求体单次读取、URL 修复/错误分类、数字与特殊集号、未剥离请求缓存键不变；测试后已删除临时文件。`node --check`、IDE diagnostics、`git diff --check` 与 Wrangler dry-run 均通过。下一批再拆鉴权与边缘缓存，不跨文件。

第二批已处理：新增 `authorizeClientIdentity` 和 `authorizeRequest`，将 `checkAccess` 频率限制、访问控制拒绝、用户归属/名单、客户端签名按原顺序收口；阶段返回 `{ accessCheck, response }`，成功时完整保留 `uaConfig` 给回源配额和密钥选择使用，失败时维持原状态码、响应体、CORS 头、内部日志和指标。临时依赖桩契约测试覆盖无 UA 放行、拉黑前置短路、归属失败计数与触发拉黑、用户→签名顺序、签名失败、白名单参数透传、访问拒绝不进入身份阶段、对外 IP/UA 脱敏；测试后已删除临时文件。`handleRequest` 已缩短至约 1013 行，语法、IDE、Wrangler dry-run 和差异检查通过。下一批处理边缘缓存。

第三批已处理：新增 `createCacheContext`、`tryMemoryApiCache`、`tryNegativeSearchCache`、`tryLocalApiCache`、`tryCommentEdgeCache` 与统一入口 `tryEdgeCaches`。缓存顺序保持为内存→负缓存/别名→本地 API 缓存/别名→R2/本地弹幕缓存；`cacheContext.memCacheKey` 固定为别名改写前的键，别名改写通过返回值显式更新 `tUrlObj/url/aliasRewritten`，本地命中仍用整季原文回填内存、裁集只影响当次响应。临时源码契约测试覆盖 match 键、非法 JSON、内存裁集、EMPTY 双重校验、两段别名串联、本地回填、R2 命中和本地弹幕回填及阶段顺序；测试后已删除。`handleRequest` 已缩短至约 766 行，语法、IDE、Wrangler dry-run 和差异检查通过。下一批处理回源配额、密钥选择与重试。

第四批已处理：新增 `tryOriginQuotaFallback`、`tryKeyPoolFallback`、`forwardWithKey`、`forwardUpstream` 与 `tryUpstreamRateLimitFallback`。回源配额仍在所有边缘缓存 miss 后检查，超限时保持“过期缓存优先、否则 `200 + errorCode:429`”；密钥池耗尽仍先尝试本地别名兜底；真实转发保持原签名头、转发 UA 覆盖、GET/HEAD 无 body、非 GET 复用已读请求体，上游限流仅允许 GET 换钥重试一次，并继续记录每个受限密钥。上游最终 429 的本地别名兜底独立收口，仍保留原上游 `errorCode` 供后续日志使用。临时源码契约测试覆盖配额放行/过期缓存/限流响应、密钥池别名命中/失败、签名转发、POST 已读 body、GET 换钥及 429 别名状态转换；测试后已删除。`handleRequest` 已缩短至约 568 行，语法、IDE、Wrangler dry-run 和差异检查通过。下一批处理响应指标、日志、缓存写入与最终响应构建。

第五批已处理：新增 `recordUpstreamResponse`、`writeUpstreamCaches`、`tryPersistentRateLimitFallback` 与 `finalizeUpstreamResponse`。回源响应指标、状态分布、出流量和内存日志保持原先记录时机；弹幕响应日志仍只输出条数。成功响应缓存写入统一收口，继续区分有效响应、真实空搜索负缓存、match 专用键/完整请求上下文、弹幕 R2 与本地端归档；异步写入仍交给 `ctx.waitUntil`，不阻塞客户端响应。真实上游 429 仍按“通用本地缓存→弹幕持久化”顺序兜底，最终响应继续合并 CORS、`X-Cache`、裁集和别名改写头。本批检查时还发现并修复了第四批清理遗留的 `matchCacheKeyOf()` 运行期引用，改为显式使用 `cacheContext.matchCacheKey`。临时源码契约测试覆盖指标日志、有效/空搜索缓存、match 键与请求体、R2/归档、两级 429 兜底及最终响应头；测试后已删除。`handleRequest` 已缩短至约 346 行，语法、IDE、Wrangler dry-run 和差异检查通过。

### 4.2 `ede.js` 高变化模块拆分（跳过）

处理决定：按用户要求跳过，本轮及后续不修改 `ede.js`；除非以后出现明确缺陷或高频改动风险，不再以文件体积为由主动拆分。

### 4.3 后端别名服务职责收口（未完成，P0/P1）

当前证据：`alias_external_service.py:262-382` 仍直接查询 `MediaAlias`、`MediaLibrary`、`EpisodeLink` 并手工组装本地 429 响应；虽然已正确限制 `status=approved`，但尚未复用 `EntityAssembleService`，线上别名解析仍有多个入口。

建议保持最小改动：提取唯一 `AliasResolver` 作为线上解析入口，让本地兜底只负责“解析 canonical/anime_id → 调用统一组装器”。暂不拆 Repository/DTO/领域事件层。

### 4.4 `external_control.py` 按领域拆路由（未完成，P1）

当前仍为约 706 行单文件，诊断、数据查询、日志、业务核查和 token 路由混在同一 `APIRouter`（`data-center/src/api/v2/endpoints/external_control.py:26-706`）。建议拆成 `ext_diag.py`、`ext_logs.py`、`ext_data.py`、`ext_biz.py`，主路由只统一 include；MCP docstring 提取与 URL 契约保持不变。

### 4.5 `main.py` 只做应用装配（未完成，P1）

`create_application` 仍内嵌静态资源、favicon、fallback 与 SPA catch-all（`data-center/src/main.py:117-333`）；lifespan 的服务启停仍逐项手写。建议先提取 `setup_static_frontend(app)`，再用简单列表/helper 管理成对 start/stop，不引入额外框架。

## 5. P2：前端瘦身（部分完成）

已完成：Dashboard insights 已移除废弃 `hours=24`；后端 `query_day` 已返回 `stat_date`；多数饼图/柱图使用 `echarts.getInstanceByDom(...) || echarts.init(...)`，卸载时统一 dispose。

仍未完成：
- `Dashboard.vue` 仍约 604 行、状态和请求集中在单组件，可按真实职责拆 `useDashboardData`、`useEChartsRegistry`、`useSystemPolling`，但优先级低于 P0/P1 后端项。
- `loadSystem/loadInsights/loadTrends/loadGeoMap` 仍有多个静默 `catch`（`Dashboard.vue:294-299,398-432,435-461,519-532`），应至少 `console.warn` 并显示“数据不可用”。
- 趋势图仍直接 `echarts.init`（`Dashboard.vue:446`），刷新时应复用已有实例。
- `web/src/utils` 目前只有 `api.js`；日期和字节格式化仍在多个页面重复，尚未新增 `utils/format.js`。
- `package.json` 的 `lint` 固定带 `--fix`，应补一个不修改文件的 `lint:check` 供 CI 使用。

## 6. P0/P1：正式测试与交付门禁（已完成）

已建立可持续回归测试：
1. `data-center/tests/test_alias_search_fallback.py`：保护裸系列词多季度聚合、罗马/中文季度指定，以及 pending 别名不进入线上结果。
2. `data-center/tests/test_db_patch_runner.py`：保护补丁幂等、失败后重试和不可逆补丁显式开关。
3. `worker/tests/local-search-fallback.test.mjs`：保护本地别名兜底的统一 dandanplay 响应结构，以及空结果、RPC 失败时继续原 429 流程。

前端新增 `.eslintrc.cjs` 和只读 `lint:check`，并修复 3 个未使用变量；CI 不再通过带 `--fix` 的命令修改源码。

`.github/workflows/docker-build.yml` 已增加独立 `quality` 作业，顺序执行 Python compile + pytest、Worker `node --check` + `node:test`、前端 `lint:check` + build；Docker `build` 作业通过 `needs: quality` 前置依赖，检查失败不会构建或推送镜像。镜像在非 PR 事件中同时发布到 Docker Hub `docker.io/${{ github.repository }}` 与 GitHub Container Registry `ghcr.io/${{ github.repository }}`，部署 compose 继续默认使用 Docker Hub 镜像。

本地验收：Python 6 项通过；Worker 2 项通过；前端只读 ESLint 和构建通过；`git diff --check` 与 IDE diagnostics 通过。Pydantic V1/SQLAlchemy 旧 API 警告和前端大 chunk 警告均为既有非阻塞项，后续单独治理。

## 7. 当前剩余优化顺序

1. **P0/P1：别名解析与响应组装统一入口**（4.3，同时完成 2.1/2.3 的结构收口）。
2. **P1：运维页容量预测**：给 `control_messages`、`api_cache_access_logs` 展示估算每日增长量和预计占用（3.1 尚未完成）。
3. **P1：拆 `external_control.py`**（4.4）。
4. **P1：整理 `main.py` 应用装配**（4.5）。
5. **P2：Dashboard 错误可见性、图表实例复用和公共格式化工具**（第 5 节）。
6. **跳过**：`ede.js`；除非以后有明确缺陷，不主动拆分。

## 8. 明确不建议

- 不重写 Worker、不切微服务、不引入消息队列/Elasticsearch/Kafka。
- 不为所有服务增加 Repository/DTO/领域事件层。
- 不修改或拆分 `ede.js`（用户已明确跳过）。
- 当前运行态健康，不靠盲目加线程、连接池或缓存掩盖结构问题。
- `handleRequest` 已从约 1213 行降至约 346 行并完成契约验证；不再为了机械达到 250 行继续拆低收益分支。

## 9. 验收指标（更新）

- P0 兜底用例进入正式测试并全绿；两种 429 路径响应 schema 一致。
- 容量运维页能看到高增长表的日增长估算和预计占用。
- Worker `handleRequest` 保持约 346 行且阶段职责清晰，不以单一行数作为继续拆分理由。
- CI 对每次提交执行语法、正式单测、前端只读 lint 和构建。
- 线上保持事件循环延迟 <50ms、队列无持续增长、连接池使用率 <80%。

